import React from "react";

const page = () => {
  return (
    <div className=" w-full h-full">
      find the onboarding card in the onboarding card component and the task
      page that will soon be edited out of it
    </div>
  );
};

export default page;
